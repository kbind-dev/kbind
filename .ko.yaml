baseImageOverrides:
  github.com/google/ko: golang:1.26.5

builds:
- id: konnector
  dir: ./cmd/konnector
  ldflags:
  - "{{ .Env.LDFLAGS }}"
- id: backend
  dir: ./cmd/backend
  ldflags:
  - "{{ .Env.LDFLAGS }}"
